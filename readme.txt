Steps
1. open the project in vscode and run "npm install" in terminal
2. to run test with report npm run report use "npm run report" in terminal
    or 
    you want to open cypress dashboard use "npx cypress open"
